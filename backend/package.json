{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "postinstall": "chmod +x node_modules/.bin/json-server 2>/dev/null || true",
    "prestart": "node check-node-version.js",
    "start": "node index.js",
    "dev": "node index.js",
    "setup": "./setup.sh",
    "vercel-build": "echo 'Building for Vercel'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "semver": "^7.7.2"
  }
}
